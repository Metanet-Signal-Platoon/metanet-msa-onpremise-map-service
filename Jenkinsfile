pipeline {
    agent any
    tools {
        gradle("gradle")
    }
    stages {
        stage('Jenkinsfile') {
            steps {
                echo "hello jenkins?"
            }
        }
    }
}